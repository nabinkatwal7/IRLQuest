import Dock from "~/components/common/Dock";

const Calendar = () => {
  return (
    <div className="reward-container-main relative w-screen h-screen">
      Calendar page
      <Dock />
    </div>
  );
};

export default Calendar;

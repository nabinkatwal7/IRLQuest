import Dock from "~/components/common/Dock";

const Chat = () => {
  return (
    <div className="notes-container-main relative w-screen h-screen">
      notes page
      <Dock />
    </div>
  );
};

export default Chat;
